<div class="container">
    <nav class="breadcrumbs" style="--bs-breadcrumb-divider:'&#187';" aria-label="breadcrumb">
        <ol class="breadcrumb">
            <i class="fa-light fa-house"></i>
            <li class="breadcrumb-item">
                <a asp-controller="Home" asp-action="Index">Home</a>
            </li>
            @if (ViewContext.RouteData.Values["controller"]!.ToString() != "Home")
            {
                <li class="breadcrumb-item active">
                    <a asp-controller="@ViewContext.RouteData.Values["controller"]!.ToString()"
                       asp-action="@ViewContext.RouteData.Values["action"]!.ToString()">
                        @ViewContext.RouteData.Values["controller"]!.ToString()
                    </a>
                </li>
            }
            @* If there a subpage that ends with Details is active then this code prints that out in the breadcrumb. *@
            @if (ViewContext.RouteData.Values["action"]!.ToString()!.EndsWith("Details")!)
            {
                <li class="breadcrumb-item active" aria-current="page">@ViewContext.RouteData.Values["action"]!.ToString()</li>
            }
        </ol>
    </nav>
</div>
